<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myviewdemo">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:name=".myapp.APP"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".kuaishou.MotionActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".hilt.HiltActivity">

        </activity>
        <activity android:name=".activity.FindMeActivity"> </activity>
        <activity
            android:name=".tabao.OnSellActivity"
            android:screenOrientation="sensor"> </activity>

        <service
            android:name=".service.MyService"
            android:enabled="true"
            android:exported="false" />

        <activity android:name=".activity.DimpleViewActivity" /> <!-- 通过切换进入的activity来展示相应的内容 -->
        <activity
            android:name=".navigationdrawer.CollapsingActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".jetpack.gallery.activity.GalleryActivity" />
        <activity android:name=".bottonavigation.BottomNavigationActivity" />
        <activity android:name=".jetpack.room.RoomBasicActivity" />
        <activity android:name=".jetpack.question.QuestionsActivity" />
        <activity android:name=".jetpack.nav.NavigationActivity" />
        <activity android:name=".jetpack.JetPackActivity" />
        <activity
            android:name=".LoginActivity"
            android:screenOrientation="landscape"
            tools:ignore="LockedOrientationActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
    </application>

</manifest>